# cc-history

Search Claude Code session history from inside Claude Code via MCP.

## Installation

```bash
uv pip install -e .
```

## Development

```bash
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"
pytest
```
